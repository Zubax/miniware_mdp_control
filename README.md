# Miniware MDP-M01 Python control

Documentation and examples are maintained in the `mdp_control` module docstring.
View them from the command line or through Python's documentation tool:

```sh
./mdp_control.py --help
python3 -m pydoc mdp_control
```
